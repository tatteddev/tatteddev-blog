---
const today = new Date();
---

<footer>
	<div class="terminal-footer">
		<span class="prompt">root@tatteddev:~$</span> 
		<span class="command">whoami</span>
		<div class="output">© {today.getFullYear()} TattedDev. Building the future, one commit at a time.</div>
		<div class="footer-links">
			<span class="prompt">root@tatteddev:~$</span> 
			<span class="command">ls social/</span>
			<div class="social-links">
				<a href="https://github.com/tatteddev" target="_blank" class="social-link">
					<span class="sr-only">GitHub</span>
					github.exe
				</a>
				<a href="https://twitter.com/tatteddev" target="_blank" class="social-link">
					<span class="sr-only">Twitter</span>
					twitter.exe
				</a>
			</div>
		</div>
	</div>
</footer>
<style>
	footer {
		padding: 2em 1em;
		background: var(--bg-dark);
		border-top: 2px solid rgba(0, 246, 255, 0.3);
		color: var(--neutral-light);
		font-family: 'Courier New', monospace;
		font-size: 14px;
	}
	
	.terminal-footer {
		max-width: 900px;
		margin: 0 auto;
	}
	
	.prompt {
		color: var(--neon-blue);
		text-shadow: 0 0 5px var(--neon-blue);
		font-weight: bold;
	}
	
	.command {
		color: var(--accent-lime);
		margin-left: 0.5rem;
	}
	
	.output {
		margin: 0.5rem 0 1.5rem 1rem;
		color: var(--neutral-light);
		border-left: 2px solid rgba(0, 246, 255, 0.2);
		padding-left: 1rem;
	}
	
	.footer-links {
		margin-top: 1.5rem;
	}
	
	.social-links {
		display: flex;
		gap: 2rem;
		margin: 0.5rem 0 0 1rem;
		border-left: 2px solid rgba(0, 246, 255, 0.2);
		padding-left: 1rem;
		flex-wrap: wrap;
	}
	
	.social-link {
		color: var(--neutral-mid);
		text-decoration: none;
		transition: all 0.3s ease;
		position: relative;
	}
	
	.social-link:hover {
		color: var(--neon-blue);
		text-shadow: 0 0 8px var(--neon-blue);
	}
	
	.social-link::before {
		content: '$ ';
		color: var(--neon-pink);
	}
	
	@media (max-width: 768px) {
		footer {
			font-size: 12px;
			padding: 1.5rem 1rem;
		}
		
		.social-links {
			flex-direction: column;
			gap: 0.5rem;
		}
	}
</style>
